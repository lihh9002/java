package com.study.download;

import org.apache.commons.io.FileUtils;
import org.junit.Test;

import java.io.File;

public class DownloadFile {

    @Test
    public void download()throws Exception{
//        RestTemplate template = new RestTemplate();
//        String url = "https://www.visual-paradigm.com/downloads/vpce/Visual_Paradigm_CE_Win64.exe";
//        byte[] bytes = template.getForObject(url, byte[].class);
//        FileUtils.writeByteArrayToFile(new File("uml.ext"), bytes);
    }
}
